#ifndef __Woobot__bhs_SSD__
#define __Woobot__bhs_SSD__

// SSD == Smart Subsystem Distributor. Named after fast storage in hopes that
// the name will "rub off", so to speak, on the program to make it fast. :)
namespace bhs_SSD
{
	// return 0 on success, not 0 on fail
    int processInputs(); 
}

#endif /* defined(__Woobot__bhs_SSD__) */
